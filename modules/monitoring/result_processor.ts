import type { CheckResult } from './contracts/check_result.js'

export class ResultProcessor {
  async processResult(_monitorId: string, _result: CheckResult): Promise<void> {
    // Processamento do resultado do monitoramento
  }
}
